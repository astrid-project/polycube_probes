apiVersion: v1
kind: Service
metadata:
  labels:
    app: node-8
  name: node-8
  namespace: astrid-kube
spec:
  ports:
    - name: "4000"
      port: 4000
      targetPort: 4000
  selector:
    app: node-8
  clusterIP: None
status:
  loadBalancer: {}
