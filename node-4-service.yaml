apiVersion: v1
kind: Service
metadata:
  labels:
    app: node-4
  name: node-4
  namespace: astrid-kube
spec:
  ports:
    - name: "4000"
      port: 4000
      targetPort: 4000
  selector:
    app: node-4
  clusterIP: None
status:
  loadBalancer: {}
